sed -i '/\/\/ 3. Update paths/i \
                val keys = graphics.keys()\n                while (keys.hasNext()) {\n                    val key = keys.next()\n                    if (key.startsWith("design_") || key.startsWith("temp_des_")) {\n                        val base64 = graphics.getString(key)\n                        if (base64.isNotEmpty()) {\n                            val bytes = android.util.Base64.decode(base64, android.util.Base64.DEFAULT)\n                            java.io.File(context.filesDir, key).writeBytes(bytes)\n                        }\n                    }\n                }' app/src/main/java/com/example/core/backup/WorkspaceManagerImpl.kt
