sed -i -e 's/model = ImageRequest.Builder(LocalContext.current).data(File(logoPath)).setParameter("refresh", logoRefreshKey, null).build()/model = coil.request.ImageRequest.Builder(androidx.compose.ui.platform.LocalContext.current).data(java.io.File(logoPath)).memoryCacheKey(logoPath + logoRefreshKey).diskCacheKey(logoPath + logoRefreshKey).build()/g' app/src/main/java/com/example/ui/settings/SettingsScreen.kt
sed -i -e 's/model = ImageRequest.Builder(LocalContext.current).data(File(signaturePath)).setParameter("refresh", sigRefreshKey, null).build()/model = coil.request.ImageRequest.Builder(androidx.compose.ui.platform.LocalContext.current).data(java.io.File(signaturePath)).memoryCacheKey(signaturePath + sigRefreshKey).diskCacheKey(signaturePath + sigRefreshKey).build()/g' app/src/main/java/com/example/ui/settings/SettingsScreen.kt
sed -i -e 's/model = ImageRequest.Builder(LocalContext.current).data(File(companySealPath)).setParameter("refresh", sealRefreshKey, null).build()/model = coil.request.ImageRequest.Builder(androidx.compose.ui.platform.LocalContext.current).data(java.io.File(companySealPath)).memoryCacheKey(companySealPath + sealRefreshKey).diskCacheKey(companySealPath + sealRefreshKey).build()/g' app/src/main/java/com/example/ui/settings/SettingsScreen.kt
