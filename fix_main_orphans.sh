perl -0777 -pi -e 's/        enableEdgeToEdge\(\)/        enableEdgeToEdge\(\)\n\n        \/\/ Clean orphaned temporary images from aborted quotation edits\n        cleanOrphanedImages\(\)/s' app/src/main/java/com/example/MainActivity.kt
perl -0777 -pi -e 's/    override fun onCreate/    private fun cleanOrphanedImages\(\) \{\n        try \{\n            val filesDir = this\.filesDir\n            val tempFiles = filesDir\.listFiles \{ _, name ->\n                name\.startsWith\("temp_des_"\) || name\.startsWith\("temp_lam_"\)\n            \}\n            tempFiles\?\.forEach \{ it\.delete\(\) \}\n        \} catch \(e: Exception\) \{\n            e\.printStackTrace\(\)\n        \}\n    \}\n\n    override fun onCreate/s' app/src/main/java/com/example/MainActivity.kt
