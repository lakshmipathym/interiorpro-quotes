sed -i 's/copied?.let { designPath = it }/if (copied != null) {\n                if (designPath.isNotEmpty() \&\& designPath != copied) {\n                    val oldFile = File(designPath)\n                    if (oldFile.exists()) oldFile.delete()\n                }\n                designPath = copied\n            }/g' app/src/main/java/com/example/ui/quotation/NewQuotationScreen.kt
